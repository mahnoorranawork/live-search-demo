# LiveSearchSimple Demo

A minimal React project showing a live search component with:
- Controlled input
- Debounce hook
- Mock async API
- Loading state
- Accessible input (`aria-label`)

## Setup
1. Install Node.js (includes npm).
2. Clone or unzip this project.
3. Run:
   ```bash
   npm install
   npm start
